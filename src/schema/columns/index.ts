import { columns as barcode } from './barcode';
import { columns as barcodeGenerator } from './barcodeGenerator';
import { columns as brand } from './brand';
import { columns as group } from './group';
import { columns as hashTag } from './hashTag';
import { columns as mediaCollection } from './mediaCollection';
import { columns as mercariBrand } from './mercariBrand';
import { columns as mercariTaxonomy } from './mercariTaxonomy';
import { columns as productImage } from './productImage';
import { columns as productLine } from './productLine';    

export default {
    barcode,
    barcodeGenerator,
    brand,
    group,
    hashTag,
    mediaCollection,
    mercariBrand,
    mercariTaxonomy,
    productImage,
    productLine
}